// Overwritten by .github/workflows/deploy.yml at deploy time with the deploy
// date and that day's sequence number, matching the YYYY-MM-DD-N release tag.
// The value committed here is only what local development shows.
export const APP_VERSION = 'dev';
